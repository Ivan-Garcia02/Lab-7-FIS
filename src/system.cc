#include <iostream>
#include <fstream>
#include <string>

#include "system.h"
#include "user.h"

bool System::CreateUser(void) {
  std::string name{""};
  std::cout << "Introduzca el nombre de usuario: ";
  std::cin >> name;
  
}